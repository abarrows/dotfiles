---
# docker - Sets up some basic docker defaults for virtualized environments.
- shell:
      - echo "Setting up DevOps - global docker ignore..."
- link:
      ~/.dockerignore: devops/.dockerignore
- shell:
      - open /Applications/Docker.app
      - echo "Docker desktop for m1 mac started..."
